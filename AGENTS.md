# AGENTS.md

## Cursor Cloud specific instructions

This is a skeleton repository with no application code, dependencies, or services. It contains only:
- `README.md` — project name placeholder (`nvMEalwaysN4ever`)
- `LICENSE` — Apache License 2.0
- `.gitignore` — ignores Oracle Forms compiled files (`.fmx`, `.mmx`, `.plx`)

No build system, package manager, test framework, or dev server is configured. No update script is needed. When application code is added, update this section with relevant build/run/test instructions.
